/**
 * Re-exports kept for backward compat with any remaining import sites.
 * The i18next singleton is gone — translations are now powered by next-intl.
 */
export type { TxKeyPath } from "./translate";
